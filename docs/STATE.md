# STATE - trusted handoff

> 任何 AI 打开本项目，先读这里。这里不记录强/弱模型身份，只记录可信事实、当前边界、待验证项和下一步。

## ENTRY POINT
- 当前阶段: 2026-09-20 用户确认连接说明可用，并要求代码与 README 一起发布。完整本地自动测试及 Release 构建通过，准备经 PR/CI 合并源码预览；不发布二进制、不改已安装应用。真实连接已确认，不再要求重复授权；其他历史手工兼容性验收继续开放。
- `plan.md`: 存在且执行中；2026-09-16 跨桌面修复已沿用原签名身份安装，既有 WorkBuddy Pin 的隐藏桌面→面板点击召回路径通过；Steps 1–9 已完成，Step 10 的独立 Pin 键盘召回器、跨屏高度修正及 Inbox 主动提醒均已安装为可体验预览、待用户确认，物理快捷键与其他真实交互验收继续开放；Step 11 的实现、设置入口和自动验证已完成，13 行真实兼容矩阵仍待物理快捷键执行。
- 当前分支: `feat/v2-dual-anchor-preview`；不得直接 push `main`。
- Harness 定位: 开发规范、证据纪律、边界控制、验证闭环和交接格式。模型强弱切换只是可选调度策略，不是项目状态。
- 发布口径: 只发布 GitHub 源码与 PR；不创建 DMG、PKG、预编译 App ZIP、二进制 GitHub Release、发行证书链或自动更新。每用户钥匙串内的本地身份只用于当前 Mac 的权限连续性。

## v2.0.0 · 2026-09-20 · 代码与用户说明共同发布核验

- 范围：双入口、Todo/每日提醒、划词、快捷键与窗口改进、Codex 回复提醒，以及相关测试、安装脚本和用户 README；不带个人配置、数据、私钥或构建产物。单独的 `docs/readme-attention` 分支由本次完整预览覆盖，无需另行合并。
- 本次本地证据：完整 Swift 231/231、Python 32/32、Pin 行结构 8/8、构建输出 6/6、签名迁移 19/19、安装事务 6/6；source-only 与 diff 检查通过。Release 编译、Info.plist、LSUIElement、严格验签及提醒组件资源一致性通过。
- 自动检查补强：构建测试夹具带入新增提醒组件并校验复制结果；CI 增加 Python 3.11、回复提醒、Pin 结构和构建输出回归。未以自动检查替代真实应用选区矩阵、长期多屏和全部客户端状态验收。
- 发布跟踪：`plan.md` 的 `plan-release-source-20260920`；远端 CI/合并结果以 GitHub PR 为准，尚未创建正式 v2 Tag。
- PR #1 发布验证新增发现：Swift 5.10 并发注解兼容问题已修正并由旧工具 Release 编译通过；完整 Swift Testing 在 CI 改为串行后通过，保留失败时崩溃报告。macOS 14 缺少 lockf，安装事务库新增同工具链编译的 Swift flock 后备实现，本地竞争/释放及 19+6 项安装检查通过，远端复验中。没有提高普通安装系统要求或跳过失败门禁。

## v2.0.0 · 2026-09-20 · 看懂连接状态与下一步操作

- 用户反证：先前说明混入技术细节且无法告诉用户下一步。页面现在只突出状态和一个主要操作：未连接→开始连接，等待验证→打开 ChatGPT，连接已验证→无需操作，异常→重新检查。诊断、范围、隐私和首次重开帮助按需展开；不以技术详情替代清晰主文案。
- 已定位并修复：客户端重开后把 NARC 放入 Computer Use 的 previous-notify 链路，旧识别器只看最外层导致误报。新识别器有界展开已知本机包装，保留脚本哈希/记录校验；重连不覆盖已包装链路，未知结构失败关闭。此轮没有改用户通知配置或执行原回调。
- 自动证据：Python 新回归先红 2 failures / 1 error，修后 32/32；Swift 提醒/浮层专项 26/26、0 failures；source-only 与 diff 检查通过。真实回执时间 15:53:51；安装后 UI 与只读状态均为 connected，详情展开/检查/收起通过，提醒开关保持 on。
- 正常 Debug 安装继承 NARC Dev；config `f5ea14ff…f4e4`、hooks `8aa01cd7…601c`、Assistant `46c263ee…919b` 前后相同。未修改 Hook 信任、不重启 ChatGPT、不发布。完整 QA、未连接/等待页真实操作与所有任务可靠性仍未验收；本轮交付为表达调整预览。

## v2.0.0 · 2026-09-20 · 无终端连接向导可体验预览

以下为先前阶段。用户随后已连接并收到真实回执，当前状态以上节为准，不再要求用户重复连接。

- NARC 设置 → AI 回复 → 连接回复提醒 → 阅读说明后允许。后台保留原通知命令与参数，继续转发原始事件（可能含正文），NARC 自己仅保存元数据。新接入不依赖 Hook 审核，也不修改其信任状态。
- 每代连接使用不可变脚本、独立记录和回执；尚未连接 → 等待首次回复 → 已收到回复完成通知。最后一个状态只证明收过通知，不保证每个对话、每一轮均成功。旧 Hook/preview 不冒充新连接，原工作链路保留。
- Python 28/28；Swift 提醒/浮层 26/26、0 failures；客户端自带 app-server 经生产 adapter 与模拟原回调集成 PASS。均为本轮证据，不等于真实 Computer Use 收尾或桌面端到端验收，完整 QA 留待体验确认。
- 正常 Debug 安装成功，保留 NARC Dev；严格验签、安装/构建主程序与资源 cmp、source-only 通过。真实界面检查连接入口、完整隐私说明及取消，config/hooks/Assistant/签名 marker 哈希前后不变。未替用户启用连接、未重启 ChatGPT。
- 限制：Python 3.11+、默认 Codex 配置目录；复杂配置/profile 覆盖拒绝自动修改。notify 不含开始/中断事件；真实多会话、子任务、中断、多桌面及原 Computer Use 收尾仍待验证。关闭显示不停止元数据接收。
- 下一步由用户在已打开设置中确认连接、保存工作并完全退出重开 ChatGPT，再完成一轮真实对话。证据与命令见 `docs/research/codex-completion-preview.md` 顶部阶段。

## v2.0.0 · 2026-09-20 · 无终端连接前置验证

以下为取得明确“允许”之前的历史阶段；权限阻塞已解决，当前实现与验证状态以上节为准。

- 新增可复现的 `scripts/tests/codex-notify-probe.py`，只在本机模拟 Responses 回复，不调用真实模型、不修改 config/hooks、不替用户审核 Hook、不运行已配置的通知程序；临时会话不进入用户任务列表。
- exec 和独立 app-server 协议实验均取得 `agent-turn-complete` 与会话/回合标识。原 Hook 16 项测试通过。没有安装新版 NARC、改动产品代码或完成真实桌面通知验收。
- 待确认的实现：在 NARC 内显示说明并由用户点击后修改通知配置；保留原通知程序的参数和原始载荷（可能含正文），NARC 自己只保存元数据，不上传正文。实现前必须取得明确授权，不把上一轮“继续验证”当作授权。
- 真实客户端重开、自动接入全部受支持对话、现有 Computer Use 收尾兼容、子任务/中断/乱序和界面连接状态均未验收。详见 plan 顶部阶段与研究记录。

## v2.0.0 · 2026-09-20 · 多会话回复提醒与静音可体验预览

- 最新验收：用户明确回复“验证成功”，合并卡与逐条关闭已确认；此前“待确认”为历史过程。确认后完整 Swift 回归 230/230、Python 16/16、source-only 均 exit 0。尚未配置全局 Hook，不能把合成事件体验确认当真实多会话监听验收。
- 交互：设置新增“AI 回复”，一次配置后自动接收受支持的 Codex 对话；球旁合并多个待继续项，每项可继续、忽略本轮或不再提醒，设置中可恢复并起本地别名。普通 GPT 聊天、前台当前会话识别不在已验证范围。
- 事件按会话/回合原子隔离，保存开始时间以抵抗旧 Stop 乱序；中断不复活。过期一小时不展示，最多读取 512 个文件，已读账本 1024。全局关闭/静音仅抑制展示；恢复不重放旧结果，元数据可能继续接收。
- 一次配置器打包在 App 内，复制到稳定用户数据路径；只备份/合并 NARC 的三个 hooks，保留其他字段、位置、notify 和信任记录。新定义仍需用户在 /hooks 审核并重开客户端，不替用户信任。当前真实 hooks.json/config.toml 未变。
- 本轮 Python 16/16、Swift 提醒/浮层 25/25、0 failures，source-only/语法/diff 检查通过；不是完整 QA 或多会话端到端验收。
- 初次签名被 `CSSMERR_CSP_OPERATION_AUTH_DENIED` 拒绝；续接时同身份临时探针和正常安装成功，未改 ACL、证书、信任或 TCC，恢复原因未证实。新版严格验签和安装/构建二进制、adapter 资源 cmp 通过；Assistant/marker/hooks/config 哈希不变。
- CUA 真实检查 AI 回复设置：全局与当前会话关闭 → 重启保持关闭 → 恢复开启，独立 UserDefaults 确认静音持久化；最后恢复开启、无静音。全局配置确认框取消，配置未写。两条标注测试的 preview 事件已投递，但 UI 工具只捕获悬浮球，合并卡和逐项忽略待用户确认。后续由用户配置/审核全局 Hook、重开客户端，再做双对话真实验证。

## v2.0.0 · 2026-09-18 · 单会话回复提醒预览

- 2026-09-20 更新：用户重开客户端后确认真实提醒正常，并授权扩展；下面“真实未验收/暂缓”是此前历史，不再要求重复单会话初始信任。中断/重复/多桌面等边界仍待验收。

- 2026-09-19 交接更新：用户已手动信任 UserPromptSubmit、Stop 新钩子；9 月 18 日后续真实回复未触发提醒，客户端完成通知存在但 NARC 事件文件仍为旧测试事件。重开客户端后的验证尚无结果，按用户要求暂缓；以下首次安装时的信任状态仅为历史记录，不要求用户重复授权。
- `assistant-backlog-001` 本轮只做当前会话的小实验，详细契约及测试见 plan 顶部阶段、`docs/research/codex-completion-preview.md`。
- 复用非激活浮层和位置计算；独立 ChatGPT 卡，不混 Todo 或未读数字；后台读取一份最多 8KB 的白名单事件，按回合去重、已读跨重启、1 小时过期。无新依赖、网络、TCC、AX 或窗口移动改动。
- Swift 聚焦 20/20、Python 9/9、原身份安装及签名通过；用户确认的是手工测试事件。当前客户端版本 26.901.51231，kind=codex，不外推所有普通 GPT 聊天。
- 原 notify 和 config.toml 完全未变，既有 hook 定义与位置保留；三个新 hook 尚未自动信任，审核必须由用户完成。真实结束事件、重复/中断/下一轮实机及后台多桌面仍开放。Stop 候选可能被其他 hook 续跑，不能当任务结果正确性的证明。

## v2.0.0 · 2026-09-17 · 窗口卡顿触发重新评估

- 用户反证使上一轮窗口性能改善不能成立为已验收结论；执行 plan 的 `plan-step-10-window-reevaluation`，不再继续点状调等待值。
- 本轮没有修改任何 App 源码、安装包、运行配置或个人数据，只修改 plan/STATE；PID 15512、安装签名与构建产物一致，排除旧包假设。不要以此推出所有权限或性能状态正常。
- 当前首写/多轮 AX 回读仍走主线程，Enhanced UI 兼容处理移到失败后的后台重试；与 Rectangle 首写前处理的机制存在差异。二者仅为优先待测假设，未用用户操作样本证实。
- 三秒采样 `/private/tmp/narc-window-reeval-15512-20260917.txt` exit 0，发现 SwiftUI 布局/重复动画开销但未捕获窗口移动调用；26.9% CPU 只是快照，不是空闲持续占用或卡顿根因。未把本轮每日 Todo 调度归罪为根因。
- 窗口策略聚焦测试 25/25、0 failures，真实动作基线仍缺；当前 Debug 与历史 Release 不可混作 A/B。详细检查入口、假设、参考源、实验停止条件和验收方法已写入 plan 同名阶段。
- 后续微信动作期证据见下一节；上面的三秒非动作样本仅保留为历史，不能再用“尚未确认 App/完全缺动作样本”描述现状。不整体回滚、换签名、重置权限或删除功能。

### v2.0.0 · 2026-09-17 · 微信跨屏闪动定位

- 用户确认微信副屏→主屏最严重、会消失 2–3 次；Pin 召回目前 OK，排除本轮改动范围。用户亲自操作，诊断未修改窗口、安装包、签名或个人数据；仓库仅更新 plan/STATE。
- 当前真实目标 PID 21651 / 窗口 148128，Enhanced UI 开启；另一个同 bundle 微信实例 PID 22514 关闭，不能混淆。30 秒窗口坐标采样 1,114 轮、exit 0，同一窗口的宽度反复从 735 跳回 900 多，再被改回，位置也回摆。采样点仍 onscreen、alpha=1，不把用户所见闪烁等同已证实的隐藏/最小化。
- 20 秒动作期 `sample` exit 0：16,161 个主线程样本中 8,254（51.1%）在同步读取微信焦点窗口的 AX IPC 等待，另有首次 frame 写入等待。兼容队列捕获完整重写、AX 等待及 main.sync 等待，主线程捕获锚点纠正。响应瓶颈已有运行态证据，不再只是源码猜测。
- 高置信原因链：首写未先处理 Enhanced UI 动画，早期回读/重试/锚点纠正与动画中间态相互干扰；同步 AX 调用阻塞主线程并拖延后续事件。首写前兼容保护是否足以消除闪动仍需单变量 A/B，不宣称已经修好或已证明每次闪现对应哪次写入。
- 证据：`/private/tmp/narc-wechat-move-sample-20260917.txt`；`/private/tmp/narc-wechat-geometry-tail-20260917.log`（仅 t≥21s 尾段，前段工具输出被截断，非完整日志）。没有物理按键时间戳或像素录像，不能给单命令 P95 或用整个片段时长代替动作耗时。
- 下一步建议先只验证首写前兼容保护、成对恢复原辅助状态，不永久禁用辅助能力、不动 Pin；之后才单独评估主线程隔离及有界 IPC。详细证据、停止条件和未来防回归测试见 plan 的微信跨屏定位节；本轮没有新增 App 测试或实施修复。

### v2.0.0 · 2026-09-17 · 首写前兼容实验已可体验

- 用户明确确认后，仅在 `AXWindowHelper.setFrameFast` 的原 frame 写入外复用 `withTemporarilyDisabledEnhancedUI`：增强状态开启才临时关闭并等待既有 20ms，写入后 defer 恢复。共享 helper、Pin、WindowManagerService、写入次数及后续校验不变；未做主线程隔离，响应阻塞风险仍在。
- 自动证据：更新结构回归先红（缺少保护，1 test / 1 issue），接入后窗口专项 25/25、0 failures；与本轮备份的源码对照、`git diff --check` 通过。不是全仓回归或真实流畅度证明。
- 原安装路径已用正常安装事务更新并通过 CUA 启动；Debug 与基线配置相同，稳定 signer 未变，严格验签及 build/安装二进制 cmp 通过。安装前后 assistant 数据、Pin 和 marker 哈希相同；没有新增授权操作或改个人数据。
- 可恢复旧版及原源码：`/private/tmp/narc-initial-compat-baseline-a1tr6j`。不要直接运行该临时路径 App；若撤销，应正常退出后恢复到原安装路径、重新验签，保留个人数据，不整体回退 Git。
- 后续用户反馈“可以了，发布一个版本”，记录为当前体验确认；未取得改后逐帧轨迹或 P95，不能升级为所有窗口性能问题均已解决。后续若复发继续按 Anti-Wall 处理。

### v2.0.0 · 2026-09-17 · 源码发布预检

- 全量 Swift Testing 220/220、0 failures；签名迁移策略 19/19、安装事务 6/6、source-only 与 diff 检查通过。发布构建、完整差异审阅和远程 CI 尚未执行。
- 67 个未提交/新增条目包括多个先前功能；已询问是否按候选 `v2.0.0-rc.1` 整版源码预览发布，尚未得到范围答复，不创建版本。
- GitHub CLI 401，但已有 connector 能识别当前用户和仓库写权限；无需立即重配凭据。当前未执行远程写入，Git 推送链路仍待验证。详情见 plan 发布预检节。

## 当前可信事实
- 产品入口固定为顶部菜单栏 + 桌面悬浮 `N`，默认无 Dock；`⌃⌥N` 把悬浮球召回鼠标所在屏幕并保持面板展开。
- 随手记录默认进入 Inbox：Return 保存，待整理记录默认折叠为数量摘要，展开后显式转 Todo/Note；明确是任务时，`⌘Return` 或 Todo 按钮可直接创建；面板、`⌃⌥Q` 与 Assistant 共用同一份草稿状态。有可处理 Todo 时“下一件事”位于输入区之前，没有时输入区优先。
- Todo、Notes 与 Inbox 保存到 `~/Library/Application Support/NARC/assistant-v1.json`；文件名保持兼容，当前源码 schema 为 v4，可读取 v1/v2/v3，仅首次保存时迁移。新增 priority、dueAt、isNext；`deferredUntil` 仍用于单任务延期。
- 存在可处理 Todo 时，悬浮 `N` 左下角显示独立蓝色数量 cue；应用未读数字语义不变。主面板保留单任务卡；主动回顾改为自定义每日时间，先突出最优先的一项，再列最多三项其他任务，不自动打开主面板、不抢焦点且不依赖 AI。
- 应用未读只读取 macOS LaunchServices 的应用级 Dock Badge；多实例取最大可信值，未知时在最后可信值后追加 `?`，无旧值时显示 `?`。
- 启动期不主动请求辅助功能或通知权限；划词或窗口动作首次使用时解释并只触发一个系统授权入口，开启后每 3 秒自动复核、通常无需重启，通知仅在首次真实投递时请求。
- 划词 Todo 的产品边界已锁定：默认 `⌃⌥T`，触发时锁定前台 App 与焦点控件并立即发起一次标准 AX 选区读取，长文先确认，成功后按精确 UUID 撤销；不持续监听，不碰剪贴板，不模拟 `⌘C`，不使用 OCR、AI 或外部服务。
- Pin 键盘召回的产品边界已锁定：默认 `⌃⌥P` 打开独立选择器并立即接受 `1–9`、方向键、Return 与 Esc，默认 `⌃⌥⇧P` 标记/取消当前精确窗口；两项可在偏好设置修改并立即生效。数字顺序只来自 Pin，不受监控 App 数量影响，本次召回目标固定为打开选择器时的鼠标屏幕。
- App Bundle 版本为 `2.0.0 (2)`；当前仍是开发预览，没有 v2 Tag 或二进制 Release。
- 普通安装会继承目标位置已有、严格有效且私钥可用的 `NARC Dev` 或 Local v1；干净用户才创建 Local v1。marker 与稳定 App signer 冲突时停止，早期误迁移只允许完整指纹确认的显式恢复；CI 与显式开发构建仍使用 ad-hoc。
- 历史自动基线：2026-09-15 完整 Swift Testing 161/161、9 月 16 日 Pin 专项 25/25；签名迁移策略 19/19、安装事务 6/6 与既有 Harness 门禁证据仍可追溯。当前自动验证状态见下方；真实物理快捷键、双屏、多窗口与跨 Space 体验不能由自动测试代替。
- 已归档一批不可信中间产物到 `docs/archive/stale-harness-v1/`，仅隔离，不删除。
- 当前业务视角功能清单记录在 `docs/FEATURES.md`；它只描述用户可见能力状态，不作为 bug backlog 或实现计划。
- 高频无权限 UI 验证可使用 `NARC_DEV_NO_AX=1`；该模式保留 `⌃⌥N` 与 `⌃⌥Q`，跳过 AX 窗口能力和系统通知。
- 本轮（2026-09-17）已把 15 项全局动作收敛到同一套可配置注册链路：10 个窗口布局、Pin 召回/标记、悬浮球召回、快速记录和划词 Todo。布局仅允许停用/启用，停用会释放组合；非布局动作不能被停用。窗口布局卡片现在只负责配置，不会因点击而移动当前窗口。
- 最新专项为 2026-09-17 快捷键/权限/路由/窗口 104/104、0 failures；本次增加 8 条设置展示、5 条写入策略/回执/回退保护测试，以及 8 项 Pin 结构检查。较早完整 Swift Testing 188/188 是历史结果，本次预览未再跑全仓；当前真实验证范围见下方阶段，卡片直达和物理快捷键/多屏仍待体验确认。
- 以下历史文档仍可作为背景参考，但不是当前 bug 修复的自动指令:
  - `docs/PRD-claude-tab-monitor.md`
  - `docs/blueprint-claude-tab-monitor.md`
  - `docs/reviews/claude-tab-monitor-2026-06-10.md`
  - `docs/PRD-workspace-ux-polish.md`
  - `docs/blueprint-smart-copy.md`
  - `docs/blueprint-widget-drag-fix.md`
  - `docs/reviews/smart-copy-drag-fix-2026-06-18.md`
- `docs/archive/stale-harness-v1/` 内文档来自旧 Harness 中间状态，后续不得直接当作执行蓝图。

## v2.0.0 · 2026-09-17 · 每日待办回顾与任务安排

- task: `plan-step-10-daily-review`；Standard 可体验预览。用户确认每日多个自定义时间、先最高优任务后其他 Todo，此决策替代历史固定两小时/每日三次策略。
- 偏好设置新增“提醒”：预置 11:00、14:30，增删修改、总开关、只读预览；设置与提醒账本同用户本机保存，隔离开发数据使用瞬时设置与账本。
- Todo 每行固定“安排”按钮：可选择唯一下一件事、高/普通/低、可选截止时间。排序为手动下一件事 > 优先级 > 同级截止早 > 新记录；完成/延期中的任务不进入主动推荐。指定下一件事取消该任务延期，完成清除指定；全部通过原 Store 原子保存。
- 主动回顾独立非激活 360×370 卡片：重点任务、其他最多三项、未完成总数（含延期但不推荐）；完成、一小时后整次回顾、直达 Todo、收起。预览禁用完成/延期，且不消费正式提醒账本。
- 本地时间按日计算，五秒调度检查；同一时点只消费一次，忙碌/休眠最多补最近十分钟的一次，过期不补；键盘最近两秒有输入、NARC 管理窗口可见、锁屏/休眠或拖球时不弹。暖机三十秒、操作静默十五秒、展示三十秒；不沿用旧两小时冷却和每日三次限制。
- 自动基线 74/74；最终 Todo/Assistant/Capture/浮球/路由聚焦 103/103，0 failures，exit 0；含 schema v3 只读加载及首次保存迁移、原子失败、唯一下一件事、排序、设置持久化、调度去重、补提醒、跨日、DST、非激活面板与预览结构回归。命令见 plan 同名阶段。source-only、diff 检查 exit 0。
- 原身份普通安装 exit 0，最终 PID 15512；严格验签、构建/安装二进制 cmp 均 exit 0，leaf 5ACFA8… 未变。Assistant `734272ca…2508973`、marker `ddd6a87f…065aaa`、Pin 偏好 `647cc6a7…87945` 哈希前后不变；无权限重置或数据清理。
- 真实 UI：时间新增/删除、开关 off/on、重启后 11:00/14:30 保留通过；安排入口、截止日期展开与取消有截图，个人任务未写入测试改动。预览曾同时保留主面板，已收起并补自动防回归；窗口探针证明曾显示 360×370 非激活卡且前台仍为其他 App，但最终 CUA 只选择悬浮球，尚无完整卡片截图/按钮往返证据。
- 未进入完整 QA：后台卡视觉、查看全部/完成/回顾延期的真实往返、到点/长时间运行、多屏和休眠继续待确认，不由单元测试替代。未提交、推送或发布。

## v2.0.0 · 2026-09-17 · Pin 命中稳定与快捷键信息收敛

- Pin 根因已由源码与截图对照确认：hover 时将单个非交互图标替换为双按钮，删除占据原 Pin 坐标。共享行现在使用独立的正文召回按钮及固定 28×28 的长期保留/移除按钮；保留现有数字/Return 路由，不让删除按钮接管键盘选择。`scripts/tests/pinned-window-row-structure-tests.sh` 修前失败、修后 8/8；真实鼠标 hover/Pin 状态往返尚未执行，不能宣称点击问题已现场验收。
- 快捷键设置复用 DisclosureGroup，全局 5 项默认展开、布局 10 项默认收起；正常状态不重复组合，恢复默认收敛为同行箭头按钮。失败时仍显示实际旧绑定、未生效请求与原因；折叠摘要保留需处理数。真实箭头点击已观察到全局 on→off、布局 off→on，截图与 AX 状态一致；索引点击 disclosure 无效，改用可见箭头坐标成功。
- 窗口布局热路径复用已读取 frame：确定同屏且尺寸严格一致仅写 position，同屏 resize 写 size→position；跨屏、源屏无法确定及兼容回退保留 size→position→size。不以 8pt 验收容差决定省略写入；四个约束接受阶段均要求非精确尺寸先尝试一次完整回退，保护高度适配。回执明确标记 skipped，最终确认日志输出已读到的 frame；没有盲调等待时间。
- 自动测试 104/104、0 failures、exit 0，命令见 plan 最新 Step 10 日志；source-only、Shell 语法和 diff 检查 exit 0。独立只读检查未发现本轮新增 P0–P2，非正式 QA 或发布审查。
- 已沿原 NARC Dev 身份安装并运行 `/Users/mickmi/Applications/NARC.app`，PID 28584；严格验签、构建/安装二进制 cmp 均 exit 0，LSUIElement=true，原 leaf 5ACFA8… 保持。tccd 自身请求 28584.2 / 28584.4 的 Accessibility 均返回 authValue=2 / authReason=4；28584.3 属于 Computer Use，不作为 NARC 授权证据。
- Assistant SHA-256 `734272ca…2508973`、signer marker `ddd6a87f…065aaa`、持久化 Pin 的 defaults 输出 SHA-256 `647cc6a7…87945` 前后不变；原快捷键召回 ⌥⇧P、标记 ⌃⌥P 仍显示生效。没有重置 TCC、修改身份、删除个人记录或以用户 Pin 做测试夹具。
- 已按用户要求在本地 Brain 的 `sessions/2026-09-17/codex-narc-pin-hover.md` 记录 gotcha 并检索命中。Brain 现有无关改动未提交；本项目也未提交、推送或发布。当前保持可体验预览。

### Debug Card — 窗口拖影与慢响应再次反馈

- 错误指纹：用户仍感到窗口移动有拖影与延迟。
- 已知事实：旧布局固定等待已移除，但首写仍对所有布局同步执行三次 AX 变更；Pin/应用召回另有 50–320ms 同步兼容路径。
- 已尝试但无效：此前去除布局固定等待并验证少量 TextEdit 样本，并未消除用户所有 App 上的体验问题。
- 已证伪假设：少量样本更快不能推出全部窗口已无拖影，也不能推出达到 Magnet 的尾延迟。
- 最可能根因：重复尺寸写入、个别 App 的 AX IPC/重绘长尾，或用户触发的是尚未迁移的召回路径；后两者本轮尚无具体场景证据。
- 下一步最小探针：确认 App 与同屏布局/跨屏/Pin 召回路径，再采集现有 WindowPerf 的逐笔返回、实际落位与同场景对照。
- 停止条件：精简同屏写入后同场景仍无改善，则不继续调等待值；回到目标 App IPC/召回路径的区分检查。本轮未改 legacy summon，未验证实际动画、Magnet A/B 或 P50/P95，不宣称拖影已消失。

## v2.0.0 · 2026-09-17 · 授权误报与实际构建产物核对

- 用户截图反证：系统设置 NARC 开关开启，项目 `build/NARC.app` 仍提示未授权。现场同时运行 ad-hoc 调试版 PID 91943 与稳定安装版 PID 26762；已退出调试版，原安装版授权没有丢失。
- tccd 日志明确区分 NARC 自身请求：13:35:25 调试版 `91943.12` 返回 `authValue=0 / authReason=5`，并记录原证书 leaf 要求与 ad-hoc cdhash 不匹配；13:35:28 安装版 `26762.9` 返回 `authValue=2 / authReason=4`。不是用 Computer Use 的授权代替 NARC 的状态。
- `build-app.sh` 修正为编译与 `--show-bin-path` 使用相同参数；先确认实际二进制存在，再组装 App。隔离回归修前复现 `assembled a stale executable`，修后 6/6 通过，覆盖 Debug/Release、失败停止及实际资源目录。ad-hoc 输出不再建议裸 `open`，只展示 no-AX 开发路径；README 同步先核对运行身份的恢复顺序。
- 自动化防复发：构建路径问题已落为 `scripts/tests/build-app-output-tests.sh`；真实授权仍需严格签名 + 实际进程路径 + NARC 自身 tccd 归因同时确认。禁止再次用临时签名 UI 结果推断安装版授权，也不通过删除权限、换证书或读写 TCC 试错。
- 普通安装 exit 0，原 leaf `5ACFA8…` 和 marker 不变；宿主正常环境严格验签 exit 0、构建/安装二进制 cmp exit 0、实际 Swift 产物与安装版 Mach-O UUID 同为 `4C7FDB0E-4F9F-387D-96DA-AD8FB1C85562`，`LSUIElement=true`。受限验证环境曾报 `CSSMERR_TP_NOT_TRUSTED`，独立宿主只读复核通过；未以修改信任设置处理。
- 新安装 PID 51808 自身的 `51808.4` 和重启 PID 58304 自身的 `58304.4` 均为 Accessibility preflight `authValue=2 / authReason=4`。最后仅运行 `/Users/mickmi/Applications/NARC.app` 的 PID 58304，不需要用户重新开启授权。
- 真实设置页显示全部 15 项已注册；左半屏从启用 → 停用 → 修改为临时 `⌃⌥⇧←` → 重启仍停用且组合保留 → 恢复 `⌃⌥←` → 重新启用，UI 与独立偏好读取吻合。原 Pin 召回 `⌥⇧P`、标记 `⌃⌥P` 保持不变；Assistant 文件 SHA-256 前后均为 `734272ca…2508973`，marker SHA-256 前后均为 `ddd6a87f…065aaa`。
- 自动操作本轮未能观察到悬浮球/召回按键展开面板，原因未定；已停止重复尝试。上述设置页往返不冒充 Windows 卡片直达、实体按键或多屏验收，Step 10 保持开放。未提交、推送、发布或改动 TCC/钥匙串权限；Harness 事件回写因 outbox 写入受限失败，不影响本地证据。

### Debug Card — 开关开启但临时版被拒绝

- 错误指纹：辅助功能开关开启，当前 NARC 仍弹未授权；新 UI 编译后仍显示旧界面。
- 已知事实：同时存在两个不同签名的 NARC；系统日志只拒绝 ad-hoc 版；构建脚本硬编码旧 `.build/debug` 作为复制来源。
- 已尝试但无效：换 scratch 构建目录后打开产物 → 仍是旧程序且触发授权拒绝。
- 已证伪假设：不是用户忘记授权；“编译缓存导致旧 UI”被复制路径回归反证。
- 最可能根因：授权症状来自误启动另一签名身份；旧 UI 来自编译位置与组装来源不一致，二者不能混为一个权限 Bug。
- 下一步最小探针：正常安装复用原 signer，核对新进程自身权限，再验证 Windows 卡片是否为新实现。
- 停止条件：原签名新版仍被拒绝或安装身份发生变化时停止，保留原 App；不得继续重置授权或换签名。

## v2.0.0 · 2026-09-16 · 窗口快捷键按个人习惯修改

- 偏好设置 → 快捷键 → 窗口召回与标记，提供两项独立编辑、修饰键与主键选择、应用及恢复默认。至少两个修饰键，不增加全局录键监听、权限或依赖；其他固定动作和布局不开放自由组合。
- 候选注册成功且旧注册安全释放后才更新配置；内部双向冲突和系统失败保留原组合，失败清理残留 slot 不复用。标记仍同步捕获目标窗口，异步召回在换键/停止后失效；运行时提示只显示已注册组合，失败明确指向设置。
- 本轮聚焦回归 55/55、0 issues（包含新增 15 条快捷键模型/事务测试），不是新的全仓回归结果；复现命令在 plan 自检日志。首跑唯一失败是无序 Dictionary 清理顺序的测试假设，纠正断言后通过；独立审查最终无 P0/P1。
- 最终预览以原 NARC Dev 身份安装到 `~/Applications/NARC.app`，PID 26762；严格验签、构建/安装 cmp、LSUIElement 和 marker 均核对。Assistant 数据安装前后 SHA-256 均为 `734272ca…2508973`，没有改动 TCC 或钥匙串权限。
- UI 检查期间用户已自行设为召回 `⌥⇧P`、标记 `⌃⌥P`；再次启动后 UI 与独立持久化读取均保持该组合。外进程 exclusive 注册探针均返回占用 -9878，而旧 `⌃⌥⇧P` 已释放；未覆盖回默认或改变用户选择。该证据证明保存/重启/系统注册状态，不冒充 agent 全程执行修改、物理按键触发或全部多屏体验。
- 保持可体验预览：真实冲突弹窗、恢复默认点击及物理按键召回仍待用户确认；Carbon 不能保证检测第三方非排他注册/局部快捷键，极端原始事件跨两代 slot 迟到的歧义尚未复现。未提交、推送或发布，Step 10 不勾选完成。

## v2.0.0 · 2026-09-16 · 原身份安装与隐藏桌面召回验证

以下为本日较早的安装证据；最新快捷键预览状态见上方新增阶段。

- 用户确认可处理原签名私钥使用权限后，本轮未修改 ACL 或 partition list；同一 NARC Dev 身份的直接签名与普通安装内签名均成功。只证明当前签名路径可用，不宣称已永久修复此前授权拒绝，也不能把打开“钥匙串访问”与恢复建立因果关系。
- `NARC_SKIP_LAUNCH=1 bash scripts/install.sh` exit 0，随后真实 UI 启动安装版 PID 50233。严格签名校验、构建/安装二进制一致性均通过，Designated Requirement 继续锁定原 leaf `5ACFA8…`，`LSUIElement=true`，signer marker 未变；个人 Assistant 数据安装前后 SHA-256 均为 `734272ca…2508973`。
- 新进程自身的 Accessibility preflight（tccd msgID=50233.4）返回 authValue=2、authReason=4；不借用 Computer Use 的权限结果，没有重新授权、重置 TCC、换证书或读取私钥/密码。
- WorkBuddy 既有 Pin 无需重新标记：点击前原 CGWindowID 112625 存在但 on-screen=false、AXWindows=0；通过悬浮球面板 Notifications 点击 WorkBuddy 后，同一 ID on-screen=true、AXWindows=1、focused=true，且 WorkBuddy active=true。该证据覆盖隐藏桌面的真实面板点击路径，不等于物理快捷键与所有多屏/全屏组合通过。
- 本轮 Pin 专项重新执行 25/25、0 failure；当前交付为已安装可体验预览，Step 10 保持开放，未提交、推送或发布。
- 复发处理规则：errSecInternalComponent 不等于钥匙串锁定。先核对锁定状态与精确身份，再以相同用户和签名参数做最小签名探针；若正规 GUI Terminal 成功而工具上下文失败，应继续查安全会话/执行环境。不得盲目更换 signer、重置 TCC 或对整个登录钥匙串批量改 partition；只有拒签可稳定复现时才考虑经确认的单私钥权限修复。

## v2.0.0 · 2026-09-16 · 跨桌面找回修复与安装阻塞（历史）

以下为本日上午的诊断与安装失败记录；安装状态已由上方实测更新，失败证据保留。

- 用户确认 WorkBuddy 未重启，只在另一个桌面。宿主只读探针证明原 Pin 的 CGWindowID 112625 仍存在、App 正在运行，但 AXWindows 一次返回 success + 0 窗口；说明“请重新标记”不能作为该场景的默认诊断。后续激活探针观察到同一 ID 可见，但当时 AX 列表已恢复，不能冒充完整隐藏桌面修复验收。
- 修复源码采用精确窗口 → 原屏显现 → 打开所属 App 的分级结果；初始可见且可确认非原生全屏的窗口才移屏，隐藏桌面保持原位置。移除 Pin 的 AppleScript 桥和私有 CGS 废弃路径，不再依赖 CG 窗口标题，也不新增权限或依赖。
- 精确成功要求前台 App、focused CGWindowID 与 on-screen ID 同时吻合。运行中 2 秒、冷启动 10 秒有界轮询，回调经过 generation / finished 检查，完成只投递一次。应用级兜底不会恢复旧 App，不会重绑 Pin；已激活却无可见窗口单独反馈。
- 验证：修正前 Pin 相关基线 15/15；修正后专项 25/25、0 failure，包含可见性误判、原屏显现、应用级兜底、隐藏/全屏不移动、空标题精确 ID 和晚到 completion。Release 编译与链接成功；source-only 和 diff 检查 exit 0，plan audit 8 pass / 0 warning / 0 fail。
- 安装未成功：普通安装在签名阶段 exit 4，底层为 errSecInternalComponent；系统日志进一步记录 CSSMERR_CSP_OPERATION_AUTH_DENIED / errSecAuthFailed。只读状态证实登录钥匙串 unlocked=true，现有 NARC Dev identity 仍存在，因此不能要求用户单纯解锁解决。
- 安装器在替换之前停止；原已安装 App 严格签名验证 exit 0，并已由真实 UI 恢复运行。未变更钥匙串访问控制、身份指纹、TCC 或个人数据；本次代码尚未安装，WorkBuddy 完整 NARC 用户路径仍待验收。

## v2.0.0 · 2026-09-15 · 固定窗口键盘召回状态（历史）

以下为 9 月 15 日交付记录；精确失败即停止、AppleScript 桥和安装态口径已由 9 月 16 日阶段修订。

- 产品与架构已确认：Pin 从 Notifications 内的附属列表升级为独立 `PinnedWindowSwitcher`；`⌃⌥P` 负责打开选择器，`⌃⌥⇧P` 负责标记/取消当前窗口。即使只有一个 Pin，首版也显示选择器，不静默直达。
- 选择器必须在打开时捕获鼠标所在显示器，并立即接受 `1–9`、方向键、Return 与 Esc，不要求先点击、切换 Tab 或进入键盘导航模式。数字只映射 Pin 的稳定现有顺序；第十项不占用全局数字键，仍可用方向键或鼠标访问。
- 精确召回以非零 CGWindowID 为首选；只有无法取得 ID 时才允许 Bundle ID + 标题的有限回退。找不到或回退出现多个候选时必须停止并反馈，不能退化为激活同 App 的主窗口、首个窗口或任意窗口。
- 目标屏幕在选择器打开时锁定；唯一匹配的 AX 窗口会被交给目标屏召回。精确 ID 失效时不降级为同 App 的同名窗口；只有从未取得 ID 的旧 Pin 才允许唯一标题回退。同名歧义、空标题、目标屏消失或跨 Space 桥接失败都会停止并恢复先前前台 App。
- 跨 Space 使用固定 AppleScript 源码，PID 与窗口标题只通过 argv 传入；脚本在任何置前动作前要求唯一标题命中。新的召回会终止旧桥接进程并使其延迟回调失效，避免旧选择晚到抢焦点；轮询也先在主线程快照 Pin，避免与移除/标记同时遍历可变数组。
- 自动证据为完整 161/161、Pin 14/14、快捷键 11/11、面板路由 4/4；其中包含 AppleScript 只编译不执行、外部标题不进入脚本源码、非零 ID 不降级、无 ID 唯一/歧义标题、无需预点击的数字/方向键/Return/Esc、Pin-only 1–9 与第十项边界、标记/取消和满额取消。
- 最新源码已由普通事务安装到 `~/Applications/NARC.app` 并启动。真实体验仍待：内建屏与外接屏双向召回、同一 App 多窗口不串、跨 Space、失效 Pin、撤销 Accessibility 后的失败反馈，以及选择器与主面板/Quick Capture 互斥。当前不能宣称 Pin 键盘召回已经正式验收或达到发布候选。

## v2.0.0 · 2026-09-14 · Inbox 焦点与主动提醒状态

> 以下为历史实现与验证；其中固定间隔策略和旧单任务主动卡由 2026-09-17 每日回顾阶段替代。

- Compact Inbox 已改为状态驱动：存在可处理 Todo 时，“下一件事”先于输入区且成为唯一主视觉；没有可处理 Todo 时不渲染空任务卡，输入区自然成为首要动作。
- 面板不再常驻重复功能说明、记录列表和两套快捷键提示；待整理 Inbox 默认显示一行数量摘要，用户主动展开后再查看、转换或删除。
- 悬浮球 Todo cue 已改为蓝色数字，超过 99 显示 `99+`；它继续与右上应用未读 Badge 独立，不相加、不覆盖、不在球面泄露任务正文。
- 主动提醒使用独立的 nonactivating 卡片窗口，不调用主面板：一次显示一项 Todo，可完成、延后 1 小时或收起；设计目标是不激活 NARC、不抢键盘焦点、不播放声音、不触发系统通知。
- 主动提醒只保留最外层 HUD 圆角卡片；复用的“下一件事”内容在提醒态不再重复绘制蓝色背景与描边。Inbox 内的“下一件事”仍保留蓝色任务卡强调，两种呈现不会互相牵连。
- 首版固定策略为启动暖机 30 秒、Todo 变化后静默 15 分钟、展示 15 秒、展示后冷却 2 小时、每天最多 3 次。主面板、Quick Capture、Assistant 等 NARC 管理界面可见或悬浮球拖动时不展示。
- 提醒复用唯一 `AssistantStore`；完成和延期成功写盘后才关闭。持久化节流账本仅含当日起点、展示次数和下次允许时间，不保存 Todo 正文或 UUID；隔离/临时数据运行使用瞬时账本，避免消耗真实用户配额。
- 隔离 Debug 实例已真实呈现提醒卡和三个操作；点击“收起”前后前台应用均为 ChatGPT，提醒卡关闭后只保留悬浮球，证明这条首次鼠标路径没有激活 NARC 或吞掉当前焦点。完成与延期继续由同一按钮容器和 Store 写盘路径承担，但其主动卡真实往返仍留待后续验收。
- 本阶段不接入 AI、账号、网络或新权限，不使用系统通知与声音，也不增加频率设置。当前仍是可体验预览；用户确认信息层级和提醒强度前，不得把 Step 10 子项、正式验收或发布状态标记为完成。

## v2.0.0 · 2026-09-11 · 划词创建 Todo 状态

- 当前实现方向保持本地确定性：用户在其他 App 主动选中文字后按当前设置的划词快捷键，默认 `⌃⌥T`；手动 Todo 与随手记继续不需要辅助功能权限，划词、窗口排列和窗口标记需要。
- 不超过 240 个字符且不超过 3 个非空行时直接创建；其余不超过 65,536 个 UTF-16 文本单元的正常选区先确认，并继续使用读取完成后的同一快照；超过上限明确拒绝。成功提示不显示正文，短暂撤销只删除本次创建的精确 UUID。
- 偏好设置只提供固定 `⌃⌥` 的安全字母预设；切换失败必须保留旧注册和旧持久化值，按住快捷键不得重复创建。
- 标准 AX 选区读取不得降级为剪贴板、模拟复制、OCR、AI、私有 API 或无界扫描；空选区、不兼容、受保护、暂时失败、权限缺失和写盘失败分别保留真实反馈。
- [兼容矩阵](research/selected-text-todo-compatibility.md)已经按 TextEdit、Safari/Chrome、VS Code、Preview 和企微的具体界面拆分，但所有真实 App 结果仍标为待验证；当前不能宣称这些 App 已受支持。
- 分发边界没有变化：继续只发布 GitHub 源码与 PR，不创建 DMG、PKG、预编译 App ZIP、二进制 Release、发行证书链或自动更新。

## 已完成的本机真实检查
- 2026-09-16 跨桌面修复已沿用原身份安装、启动并继承 NARC 自身的辅助功能授权；隐藏桌面的既有 WorkBuddy Pin 经面板点击后精确 ID、可见性与焦点全部吻合。此次未修改持久钥匙串权限，签名间歇拒绝的根因仍未证实。
- 2026-09-15 的 Pin 键盘召回预览已通过普通事务安装并启动为 PID 77678；安装版严格签名验证 exit 0，`LSUIElement=true`，构建/安装二进制逐字节一致。该证据只证明可安装、可启动和分发边界，不代替物理 `⌃⌥P` / `⌃⌥⇧P` 与真实窗口往返。
- 2026-09-14 的单层卡片修正已用隔离临时 Todo 真实截图确认：320×188 主动提醒只剩一条外沿圆角边界，标题、计数和三个操作仍完整显示。最新版随后通过普通事务安装并启动；严格签名、稳定 Designated Requirement、`LSUIElement=true`、构建/安装二进制一致和个人 Assistant 数据哈希不变均已复核。
- 2026-09-14 的普通事务更新已把本轮最新版安装并启动到 `~/Applications/NARC.app`；严格签名有效，Designated Requirement 与 marker 均锁定 `NARC Dev` leaf `5ACFA8…`，`LSUIElement=true`，构建/安装二进制一致。个人 Assistant 数据安装前后 SHA-256 均为 `98c9b36…867d`。
- 最终安装版真实召回后，“下一件事”位于输入区之前，随手记输入框获得焦点，待整理记录默认显示折叠摘要；按 Esc 后面板关闭且 NARC 回到非前台，仅保留悬浮球。
- 恢复后 NARC PID 68113 与重开后的 PID 75872 均由 tccd 直接归因为 `com.mickmi.narc`；其 `kTCCServiceAccessibility` preflight 均返回 `authValue=2, authReason=4`。该证据在 Computer Use 介入前取得，证明旧授权已恢复给 NARC 自身，而不是由其他已授权工具代答。
- 安装器已覆盖 marker 优先、旧 signer 自动继承、稳定 signer 冲突拒绝、ad-hoc 不继承、运行中禁止覆盖和显式恢复事务；同一用户的安装由跨源码目录内核锁串行化，切换前再次核对 App/marker/钥匙串 signer，HUP/INT/TERM 及切换后验签失败都会恢复旧 App 与旧 marker。恢复过程未删除任何身份，也未读取、修改或重置 TCC。
- 一条命令安装在隔离目录完成 smoke；身份缺失、重复、不匹配或签名失败时停止且不降级 ad-hoc，source-only 门禁同时拦截 Release、artifact、发行写权限和可复用密钥材料。
- 悬浮 `N` 可见并显示检查时的应用级 Badge 29（WeChat 3 + WeCom 26）；点击后默认打开 Inbox，输入框自动聚焦，Return 保存、关闭重开后的草稿保留均已通过。
- 本轮临时测试记录已精确清理，Inbox 恢复为 0，既有 Todo/Notes 保持不变。
- 2026-09-08 的被动 Todo 闭环曾使用仅允许临时目录的 Debug 数据路径完成真实 `.app` 往返：空数据不显示 cue/空卡，多任务可 Next、完成、延期，单任务禁用 Next，`⌘Return` 只创建 Todo、Return 只创建 Inbox，延期跨进程重启仍被候选排除；验收前后个人数据 SHA-256 一致。该证据证明数据与主面板基础语义，不代替 2026-09-14 数量 cue、焦点重排和主动提醒卡的本轮体验确认。
- 窗口布局已在真实内建屏幕 + 外接 `LS27D80xU` 上完成 Carbon 快捷键验证：TextEdit 左半屏、同键跨出、反向跨回均以真实 frame 回读确认；约束窗口的 1060px 最小宽度大于 960px 半屏时，仍按实际宽度正确右贴边。按需性能日志显示这些受控样本首笔写入返回 6–61ms、最终确认 33–82ms；不记录窗口标题或内容。
- 划词功能使用稳定本地身份 Debug `.app` 与空的临时 Assistant 路径启动，日志确认 Accessibility 有效且 `⌃⌥T` 已注册；从 NARC 应用菜单打开的真实偏好设置显示“划词创建 Todo”、当前值 `⌃⌥T` 与“已启用”。Computer Use 的合成组合键不能进入 Carbon 全局热键，故没有把 TextEdit 尝试计入兼容矩阵；隔离文件未生成，剪贴板计数未变，个人数据 SHA-256 前后一致，原安装版已恢复运行。

## 已修改但待真实手测
- 9 月 16 日跨桌面修复已安装，WorkBuddy 既有隐藏桌面窗口经面板点击召回已通过；后续需用物理 `⌃⌥⇧P` 标记/取消，并执行 `⌃⌥P → 1` 的同屏、双屏、同 App 多窗口与跨 Space 往返，另外确认 Esc/关闭按钮会把焦点还给原 App。面板点击证据不代替这些独立交互验收。
- 不同高度显示器之间的半屏移动已增加跨屏专用兼容重试并安装；需用 QQ 音乐按“右屏左半 → 左屏右半”及反向各验证一次，确认目标屏全高与合法最小宽度可同时成立。同屏快速路径未改变。
- Inbox 无 Todo 时的输入优先状态、待整理记录展开后的完整操作，以及新每日回顾卡的真实完成/延期、失败反馈、自动收起、自定义时间与休眠补提醒往返；历史“收起”的不失焦证据不冒充新卡完整验收。
- 划词 Todo 的真实 Carbon 快捷键、长按去重、长文确认、非激活成功/失败反馈、精确撤销，以及偏好设置安全预设切换与冲突回滚；兼容矩阵中的 TextEdit、Safari/Chrome、VS Code、Preview 和企微具体界面均待真实 App 证据。
- Todo 延期“到点后 60 秒内重新出现”的计时往返，以及真实 `99+` / `?` 与 cue 的视觉共存仍未在人为可控状态下逐一复现；边界逻辑和组合呈现已有自动测试。
- Inbox 待整理记录的转换和写盘错误保留已有自动覆盖，展开后的真实转换与错误路径仍待手测。
- 菜单栏左右键分流与状态栏数字/未知态。
- 干净偏好下的分阶段引导，以及首次窗口动作与首次通知的按需权限路径；权限状态机与回归测试已通过，仍需真实 TCC 界面确认无叠窗、不抢焦点和 3 秒内刷新。
- 普通源码更新已在登录钥匙串可用时多次成功复用同一 `NARC Dev` 身份；此前钥匙串锁定时的安全停止证据仍有效。
- 真实双显示器悬浮球召回、重复 `⌃⌥N` 不关闭面板，以及原生全屏 Space 的窗口动作。

## 当前已知产品问题
- 9 月 9 日的窗口回归只覆盖了会同时响应宽高的 TextEdit 和最小宽度约束，遗漏了跨不同高度显示器时单一尺寸轴延迟生效的 App。9 月 15 日预览已阻止这种部分 frame 被提前确认，但 QQ 音乐双向真实 AX frame 回读尚未完成，因此不能声称该问题已经最终关闭。
- 每日回顾已按用户确认增加时间设置；提醒强度、优先级排序和其他待办的展示仍需真实体验反馈，不扩展为 AI 或复杂重复任务系统。
- 标准 Accessibility 选区是否可用取决于目标 App 的具体控件；同一 App 内正文、输入框、消息区、PDF 文本层和扫描件可能得到不同结果。在逐行实测前只能标记待验证，且禁止用剪贴板、模拟 `⌘C`、OCR 或 AI 补齐兼容率。
- 窗口布局的 50–320ms 通用固定等待及 600ms 全屏固定等待已移除，兼容回退改为专用后台串行队列，状态只在真实 frame 回读后确认。目标 App 自身的同步 AX IPC 仍有偶发长尾：实测多数受控动作在 6–61ms 返回，但 Chrome/企微忙时出现过 200ms 级样本；当前没有严格 P50/P95 或与 Magnet 的同场景 A/B，因此不能承诺所有 App 恒定低延迟。
- 早期 v2 预览已误迁移到 Local v1 的机器需要一次显式 `--restore-legacy-signer`；后续普通安装已阻止该 signer 变化。若旧私钥已经丢失，只能按当前 App 路径重新授权一次。
- 登录钥匙串锁定时，`codesign` 可能返回 `errSecInternalComponent`；安装器会在替换 App 前停止，用户需先在“钥匙串访问”中解锁“登录”钥匙串。
- LaunchServices 不保证所有 App 始终暴露 Badge；`?` 后缀表示状态不可确认，不是零未读。
- 微信/企微会话级未读没有可信公开状态源，当前明确不实现。
- Workspace 与嵌入式终端入口已软下线；残留底层只作一版回滚，不属于当前产品承诺。
- AI 编排、外部连接器和动态插件平台属于后续版本，当前不实现。

## 已隔离的旧中间产物
- `docs/archive/stale-harness-v1/PRD-backlog-v14-v15.md`
- `docs/archive/stale-harness-v1/PRD-diff-drawer.md`
- `docs/archive/stale-harness-v1/PRD-next-round.md`
- `docs/archive/stale-harness-v1/blueprint-backlog-v14-v15.md`
- `docs/archive/stale-harness-v1/blueprint-diff-drawer.md`
- `docs/archive/stale-harness-v1/reviews/backlog-v14-v15-2026-06-23.md`

## 下一步建议
1. 当前运行的是 9 月 17 日原签名快捷键预览版；请体验 Windows 卡片的勾选与改键入口，再用当前保存的召回组合（本机 `⌥⇧P → 1`）体验既有 WorkBuddy Pin，不要求重新标记。设置页往返与新进程授权已通过，物理按键、卡片直达、同屏/双屏/原生全屏继续保留独立验证；若后续授权异常，先核对运行路径和签名，不先删除权限或证书。
2. 在偏好设置 → 提醒配置时间并预览；在 Assistant → Todo → 安排选择下一件事及优先级，确认每日回顾“先重点、后列表”的体验。用户确认后再做完整到点/休眠/跨日和操作失败 QA。
3. 用隔离数据和物理 `⌃⌥T` 逐行执行 Step 11 划词兼容矩阵；只有取得真实 App × Surface 证据的行才能从“待验证”升级，全部发布 Gate 满足前保持 `assistant-022` 未完成。
4. 完成 Step 10 尚余的菜单栏、悬浮球双屏召回、原生全屏 Space、Inbox 转换/错误和干净权限/引导验收，并在可控环境补做 NARC 与 Magnet 的同窗口 A/B 和微信/企微 P50/P95；CI 与 review 无阻断后再创建 PR/合并，暂不打 v2 正式 Tag。

## 回合卡片要求
- 只要本回合有交付物、状态变化、归档、文件改动、卡点或阶段推进，最终回复必须输出回合卡片。
- 如果没有回合卡片，应视为没有完整遵守 Harness 交接约束。
